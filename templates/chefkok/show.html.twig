{% extends 'base.html.twig' %}

{% block title %}Chefkok{% endblock %}

{% block body %}
    <h1>Chefkok</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chefkok.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chefkok.name }}</td>
            </tr>
            <tr>
                <th>Leeftijd</th>
                <td>{{ chefkok.leeftijd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chefkok_index') }}">back to list</a>

    <a href="{{ path('chefkok_edit', {'id': chefkok.id}) }}">edit</a>

    {{ include('chefkok/_delete_form.html.twig') }}
{% endblock %}
