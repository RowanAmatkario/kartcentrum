<form method="post" action="{{ path('chefkok_delete', {'id': chefkok.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chefkok.id) }}">
    <button class="btn">Delete</button>
</form>
